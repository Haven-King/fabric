archivesBaseName = "fabric-config-api-v1"
version = getSubprojectVersion(project, "1.0.0")

dependencies {
}

moduleDependencies(project, [
		'fabric-api-base',
		'fabric-networking-api-v1',
		'fabric-resource-loader-v0'
])
